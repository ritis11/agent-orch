GEMINI_API_KEY=
TAVILY_API_KEY=
# Slack (optional — bot won't start without these but backend will boot)
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
SLACK_SIGNING_SECRET=
# Override default sqlite path if needed
# DATABASE_URL=sqlite:///data/app.db
